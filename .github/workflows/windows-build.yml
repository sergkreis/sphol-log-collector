name: Windows portable validation
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  portable:
    runs-on: windows-2022
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.11.9'
      - run: python -m unittest discover -s tests -v
      - run: python -m pip install -r requirements-build.txt
      - run: python -m PyInstaller --clean --noconfirm --onefile --windowed --name SPHOLLogCollector run_collector.py
      - shell: pwsh
        run: |
          $hash = (Get-FileHash dist/SPHOLLogCollector.exe -Algorithm SHA256).Hash.ToLower()
          "$hash  SPHOLLogCollector.exe" | Set-Content dist/SHA256SUMS.txt
          git rev-parse HEAD | Set-Content dist/SOURCE-COMMIT.txt
      # Deliberately no release, artifact upload, signing or publication step.
